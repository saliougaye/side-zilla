const createUploadController = () => {
	const getPresignUrl = () => {};

	const createUploadRecord = () => {};

	const createShareLink = () => {};

	return {
		getPresignUrl,
		createUploadRecord,
		createShareLink,
	};
};
